{
  "name": "parsedock",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:sidecar": "bash scripts/build-sidecar.sh",
    "kill": "bash scripts/kill-parsedock.sh",
    "tauri:dev": "bash scripts/kill-parsedock.sh && npm run build:sidecar && npm run tauri dev",
    "tauri:dev:fast": "bash scripts/kill-parsedock.sh && npm run tauri dev",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tauri-apps/cli": "^2.10.1",
    "svelte": "^5.55.1",
    "svelte-check": "^4.4.5",
    "tslib": "^2.8.1",
    "typescript": "^6.0.2",
    "vite": "^8.0.3"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-clipboard-manager": "^2.3.2",
    "@tauri-apps/plugin-dialog": "^2.6.0",
    "@tauri-apps/plugin-shell": "^2.3.5",
    "@tauri-apps/plugin-store": "^2.4.2"
  }
}
