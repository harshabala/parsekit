node_modules/
dist/
target/
sidecar/node_modules/
src-tauri/binaries/
src-tauri/gen/
.DS_Store
.brand-preview/

# Claude Code local worktrees / session state — never commit
.claude/
*.css.bak
*.log
.env

# Tauri updater signing keys — private key must never be committed
.tauri/
*.key
!*.key.pub
parsekit.key
parsekit.key.pub
src-tauri/com.parsedock.app
src-tauri/*.app
com.parsedock.app.lock
**/com.parsedock.app.lock

# macOS Finder duplicate copies (break cargo auto-discovery in src/bin/)
* 2.rs
* 2.ts
* 2.svelte
* 2.json
* 2.png
* 2.icns
* 2.ico
* 2.md
* 2.sh
com.parsedock.app
com.parsedock 2.app
**/ParseKit*.app
!src-tauri/target/release/bundle/**/*.app
